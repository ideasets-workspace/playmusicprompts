# GENERATE_MUSIC_HYBRID_MODEL — runtime dependencies
#
# Deliberately MINIMAL, and the reason is structural rather than stylistic: this worker does not call
# Vertex itself. It delegates the paid generation to the already-deployed `ssm-content-worker-music-lyria`
# (which carries google-auth, requests and the container/duration measurement code), so the Google
# client stack has NO place here — a second Vertex client would be a second behaviour to keep in
# agreement by discipline, which is the defect class this repo has measured four times.
#
# boto3 is present in the Lambda runtime, but it is pinned here because a local harness must resolve
# the same major version as the runtime rather than whatever happens to be installed.
boto3>=1.34.0
