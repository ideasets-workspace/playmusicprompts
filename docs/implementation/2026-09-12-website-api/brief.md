# PlayMusicPrompts website — live API integration

## Current owner direction — AWS EC2, quality before cost

Latest owner instruction on 2026-09-12: "maliyet önemli değil. bu projenin aws de bir ec2 su olacak". AWS EC2 is required; cost no longer drives provider selection. Prioritize security, reliability, audible continuity and the accepted experience. The prior GCP-first proposal and blanket provider-undecided gate below are superseded for EC2 participation. See [current EC2 direction](ec2-platform-direction.md) for exact owner decisions versus proposed service roles. S3/CloudFront and detailed topology remain proposals; the existing Google Cloud generation API is not ordered to migrate. No instance creation, deployment or quota expansion is implied.

Berk explicitly supplies current live API access and docs/api and asks to begin complete development. Preserve the accepted visual/player work in all three website_html_templates* trees. Implement the working website in a separate website/ directory, using the complete latest accepted frontend as the visual base. English product, Turkish collaboration. Keep local until explicit deployment authorization. Do not publish or change production IAM/API code/quotas.

Quality before speed protects Berk's finite time and money. Read the governing law, actual docs and source before implementation. Parent owns integration and actual browser verification; tests alone do not prove working media delivery. No fake generation, account success, catalog audio, progress percentages or unverified completion claims.

User reports API base https://music-api-636636169989.us-central1.run.app, authenticated health200, capabilities103, API key account website scope music:generate with60/min100/day, backend service account website-backend@playmusicprompts.iam.gserviceaccount.com already invoker. Treat as owner-supplied evidence and refresh live. Raw key only at the specifically named C:/Users/berke/playmusicprompts-website-api-key-2026-09-12.txt. Parent alone handles credentials. Never print keys/tokens, copy them into served files, logs, evidence or browser. Import into a protected local Windows secret store, verify roundtrip, then remove precisely that plaintext file as explicitly requested. Do not touch other credentials or auth configuration.

Acceptance: measured server-only dual-auth capabilities connection; current documented request fields and real capability drift reconciled; validated prompt/options and instrumental/custom lyric flows; asynchronous job state survives navigation/restart, no accidental duplicate billable submissions or pretend upstream cancellation; genuine field/quota/policy/error states; mastered delivery selected, expired delivery URLs refreshed; generated music playable and saved across approved main pages and Three.js player; previous versions unchanged; independent review plus actual local browser and bounded live generation proof.

Read-only/free connectivity calls first. At most two intentional integration smoke generations (one short instrumental and one short sung request if needed for distinct support), sequential, each logical request idempotent, no blind retries or load tests. This is a validation budget, not a new quota. Real API contract may expose unrelated account/catalog service dependencies; report rather than manufacture them. Existing unconfigured account adapters are not permission to create accounts or deploy external infrastructure.

## Prior owner clarification — 2026-09-12 (historical; EC2 direction above takes precedence)

The streaming/cloud provider is UNDECIDED. Berk explicitly says: "bak şimdi bir karar almadım ben. aws de olabilir google cloud da sen araştır öner". Historical AWS-only research is an earlier search scope, not authorization to select AWS now. Compare AWS, Google Cloud and a justified hybrid, then recommend; do not deploy or bind the product to a provider before the owner decides.

Generated music must be ingested into our own server-controlled storage, with integrity/probe evidence, then served from our own listening/radio layer. Upstream signed delivery URLs are ingestion leases, not the primary listener architecture. Retain all actual takes and original metadata. Retrying a copy or packaging failure must never repeat paid generation. Master-download objects and listening renditions have distinct permissions.

Guest listening and generation are required. Login is required for playlists and download; download additionally requires a rewarded ad. Google audio ads play between songs; the website has an unobtrusive banner. These requirements remain registered even where publisher credentials, Google's reward policy or lack of web SSV needs resolution. No simulated ads, forged-client-resistant claims from browser-only events, or silent substitution of a generation credit for the requested download reward.

Current delivery is WEB. Design shared identity/media/entitlement interfaces and suitable responsive, keyboard, touch and remote-control interaction boundaries for future Samsung/LG/other smart TVs, Apple/Android phones and tablets. Native apps and actual TV compatibility are separate implementation/verification work, not delivered by a responsive HTML page.

Security: server-only upstream credentials, durable idempotent admission, guest abuse/spend limits without forcing login, server-verified account ownership, least-privilege media access, separated masters/renditions and explicit reward-verification trust. A stream cannot be truthfully promised unrecordable. Research must identify the actual attainable protection and policy-compatible download model.

Tasks remain API-01 through API-05, with API-01-CLOUD and downstream atoms added to the dated ledger. Exact next action is provider-neutral research and recommendation. Platform-dependent implementation awaits that decision; current app creation, paid generation, secret import/deletion and cloud mutations have not occurred. Delegates own explicitly named research files only; parent retains credentials, shared memory and integration ownership.
