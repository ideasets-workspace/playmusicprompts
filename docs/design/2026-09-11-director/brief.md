# Director performance expansion

User: 'ok şimdi dünyada bugüne kadar yapılamamış seviyeye getir.' Advance the accepted Visual Studio into a real playable visual instrument; do not claim global novelty. Quality before speed protects Berk's time. Preserve all four accepted scenes, the29-setting schema/default artwork, actual audio/session/library, Motion-off freeze, English interface and local-only scope. Parent owns website integration and actual rendered/control verification. Delegates write only named files in this folder. Read full project law, project/lane records and current source before edits.

Atomic sequence: DIRECTOR-01 source/contract/backup; DIRECTOR-02 integrated performance engine, Director UI, optics/camera/capture; DIRECTOR-03 real browser testing, regression, source manifest and handoff. Do not close from source checks alone. Current task is DIRECTOR-02 once this contract is recorded.

## Engine and persisted configuration

Agent owns player-three-director.js, director-check.mjs, director-notes.md in this docs folder. Parent copies to website. Import existing production schema/presets via ./player-three-visual-settings.js in deliverable (tests adapt path). API exports DIRECTOR_DEFAULTS, DIRECTOR_SOURCES, DIRECTOR_TARGETS, sanitizeDirectorConfig, createDirectorStore(adapter), createDirectorEngine(). No DOM/RAF/render/audio.

Config exactly: {mode:'manual'|'morph'|'score',follow:false,morph:{x:0,y:0,corners:[{name,settings},{name,settings},{name,settings},{name,settings}]},routes:[{id,enabled,source,target,amount}],cues:[{id,time,label,scene,settings,duration}],scoreTrackId:null,optics:{dispersion:0,streak:0,grain:0},camera:{mode:'still'|'breathe'|'arc',amount:.35}}. Defaults four corners in order bottom-left Dreamstate, bottom-right High Voltage, top-left Deep Space, top-right Night Drive; default mode manual makes these inactive. Default4 routes all disabled: bass→bloom .25, pulse→exposure .18, centroid→saturation .25, energy→motionSpeed .3. amount range[-1,1]. Allowed sources bass,mid,treble,energy,pulse,centroid. Allowed targets bloom,exposure,saturation,particleGlow,motionSpeed,neuralSway,neuralSpread,horizonFlow,horizonTilt,prismSpeed,prismTwist,recordSpectrum (no self-feedback through audio gain controls). Export sources/targets arrays of {id,label,scene?}. Max4 routes,24 cues, name/label max48; cue time0..86400, duration0..30. Unique safe IDs, reject NaN/unsafe data; scene IDs record,aurora,orbital,liquid.

Optics bounds dispersion0..1,streak0..1,grain0..1; parent/optics module converts to subtle bounded physical shader amounts. Camera amount0..1. Existing visual schema stays29; optics persisted here only.

Store: DIRECTOR_STORAGE_KEY='pmp.room.director.v1'; adapter get(key,fallback),set(key,value)->boolean. read() no writes; patch(partialConfig) reads latest and shallow-merges top-level config; result {ok,config,error?}. Failed write returns transient candidate and honest error. Parent/controller/UI will retain transient changes. Sanitize all nested objects; initialization never overwrites. No audio/session writes.

Engine: setConfig(config), update({baseSettings,scene,trackId,time,dt,playing,motion,signals:{bass,mid,treble,energy,pulse,centroid}}) -> {settings,scene,xy:{x,y},signals,activeCueId,status,mode,optics,camera}. settings are complete29 sanitized numeric values. No side effects. Manual base exact identity unless enabled routes. Morph bilinear interpolation of four full settings (x0left/x1right,y0bottom/y1top), bounded and continuous. follow only in morph: real energy controlsx and real centroid controlsy with eased response while playing+motion; no demo signal. Manual pointer changes disablefollow. Audio routes: add smoothed normalized source * amount * .35*(schema.max-schema.min) to base/morph/scored setting, clamp; apply only matching active scene target. Source levels must derive only supplied realsignals, clamp0..1. Avoid cumulative drift by always starting from base. Pause releases audio modulation smoothly; Motion false freezes all automatic output (including score and follow) while explicit config edits/static changes can apply. A no-change Motion false frame cannot change settings/scene/xy even if fresh audio/time arrives. Default/manual/no routes remains exactly base even Motion false.

Score: bound to scoreTrackId; other track leaves manual base with clear status. Sort cues bytime deterministically, no duplicate times (sanitize/upsert chosen documented behavior). Before firstcue returns base+current scene. On/aftercue scene isthatcue.scene; settings interpolate from prior cue settings (or supplied base for first) to current cue.settings over currentcue.duration using smoothstep. Seek backwards/forward produces deterministic setting and activecue from absolute audio time; no traversal clocks. User capture stores complete current visual settings and scene at chosen real time. Mode score arms only with current track+atleast1cue in UI. No fake BPM/verse detection or automatic random scene rotation.

Meaningful checks: neutral identity, all four bilinear corners and center, source route bounds/no drift/scene applicability, pulse zero, real follow, Motion freeze/resume/config edit, pause release, cues seek/boundaries/durations/track mismatch, store malformed/failure/latest merge, resource-free engine. Tests clearly don't establish rendered quality.

## Director interface

Agent owns player-three-director-ui.js, player-three-director.css, ui-notes.md in this folder only. No website edits. Imports ./player-three-director.js and ./player-three-visual-settings.js and ./player-three-collection.js expected after parent copy. Export createDirectorControls({container,getConfig,commit,getSnapshot,getTrack,audio,getLive,captureFrame,toast}). getConfig current config. commit(partialConfig)->{ok,config,error?} applies/persists and notifies engine; getSnapshot()->{settings,scene}; getTrack()->{id,title}|null; getLive()->last engine output. captureFrame()->Promise<void> parent handles real PNG capture/download. Return {refresh(),update(live),dispose()}. No own RAF; parent callsupdate at~10Hz; update meters/labels only without rebuilding focused form inputs. Parent inserts into fourth Studio tab 'Director', keeps real livepreview above.

Design premium compact visual instrument, harmonious dark lilac/turquoise, strong English microcopy, real functional controls. Three inner views Perform / Score / Lens (or similarly cohesive structure), keyboard accessible. Perform: attractive XY pad with four named corner zones, draggable/pointercapture puck and keyboard arrows, accessible two sliders forx/y, explicit Manual/Morph enable and Follow music; cornerselects built-in look or Capture current; four editable source→target modulation routes with on/off/depth and real level feedback. Manual bypass clears automatic performance, preserves original saved29values. XY/direct controls only explicit edits persist, no writes on each audio frame. Don't store fake demo performance.

Score: atmost24 captured cues, songbound. Cue time seconds input defaults audio.currentTime; label, transition seconds, Capture cue; list supports edit time/duration/label, remove, jump via audio.currentTime with existing seek event ifneeded parent can expose. If no track, accurate empty state; no fabricated cues. If existing score belongs to another track, disable capture/arm and allow explicit clear. Arming mode score distinct from playback, never starts song automatically. Capture stores getSnapshot current effective29settings+scene. Provide currentcue indicator. Before first cue/ended/paused copy truthful. Clear score authorized local reversible.

Lens: sliders chromatic separation, anamorphic streaks, film grain; camera still/breathe/arc and amount. Explain Motion requirement/fixedview behavior clearly without implementation jargon. Capture frame button requests real PNG, disables while pending and reports errors. Reset Lens restores zeros/still only. Default neutral; current art unchanged until user operates. Scope accessible labels, switches and error feedback; support520px drawer and narrow window without page overflow. Parent performs actual UI/renderer validation.

## Cinematic optics module

Agent owns player-three-optics.js, optics-check.mjs, optics-notes.md in this folder only. Export createOpticsPass() returning {pass,update({dispersion,streak,grain,time,motion,quality}),dispose()}. Use pinned local Three ShaderPass import 'three/addons/postprocessing/ShaderPass.js' in deliverable. Host places after existing grade and before OutputPass. Defaults all0 -> pass.enabled=false, exact accepted identity. Chromatic radial lens separation up to small boundedscreenUVoffset, anamorphic horizontal highlightstreaks (not arbitrary blur of darkpixels), tasteful finegrain anchored while Motionoff. Clamp values0..1. No own RAF/renderer/audio. Qualitylow reduce shader taps viauniform/config but keep effect available; no static path that silentlyignores requestedcontrol. No texture downloads. Document shader samplebudget; real screenshot byparent. Test actual ShaderPass uniforms/default/bounds/freeze/disposal and shadersource numericproperties. Host adapts canvas pixels/resolution ifneeded define setSize oruniform innotes.

## Parent work

Integrate all modules and fourth tab. Main loop provides true feature signals and absoluteaudio time toengine, applies ephemeral settings without per-frame storage. Disabling/reloading never overwrites base29 preferences or session. Manual studio edits return Director to manual/bypass; Compare shows true Original with performance temporarily bypassed. Score scene choices don't overwrite manual scene preferences each frame. Handle manual scene selection/track changes/close. Smooth camera breathe/arc respects composedHorizon/Prism views, Motionofffreeze, manual drag priority, baselinecamera and no reset every frame. PNG capture queued immediately after a real compositor render withcanvas.toBlob (no synthetic frame); preserves liveaudio. Actual tests4scenes, routes/follow/morph, cue seek andplayback, freeze, optics, savedreload andPNG, scoremismatch, defaults, responsiveUI and sourcepreservation. No publishing.
