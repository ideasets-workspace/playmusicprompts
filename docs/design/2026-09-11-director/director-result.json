{
  "passed": 27,
  "tests": [
    "Default engine is exact complete29 identity in play, pause and Motion off",
    "All four bilinear corners are exact and center is the arithmetic mean",
    "Morph clamps coordinates and is continuous between adjacent positions",
    "Each route has bounded signed depth and never accumulates frame drift",
    "All six supplied real sources drive routes; unknown/NaN sources do not",
    "Scene-specific routes apply only to their own world",
    "Disabled, zero-depth and zero-pulse routes are exact neutral",
    "Follow uses only real energy and centroid, eases, and holds on pause",
    "Follow exits through explicit pointer configuration and updates while Motion off",
    "Motion off freezes settings/scene/xy/signals despite time and fresh sound",
    "Static base and optics edits apply while frozen without advancing follow",
    "Repeated equivalent setConfig calls do not reset follow or modulation envelopes",
    "Pause releases modulation smoothly to base and retains no accumulated offset",
    "Score boundaries, smoothstep duration and deterministic seeks use absolute track time",
    "Zero duration switches exactly; overlapping transitions follow documented previous cue settings",
    "Score Motion freeze survives optics edits and resumes at actual audio time",
    "Score missing track/cues/mismatch return exact base and correct status",
    "Explicit track changes bypass a bound score even with Motion off",
    "Before the first cue score mode is exact base even with enabled routes",
    "Sanitizer caps values, repairs IDs, strips unknowns and resolves duplicate times deterministically",
    "Sanitizer limits24 cues/4 routes and48-character labels",
    "Inputs and returned values cannot mutate config, defaults or future engine output",
    "Safe store reads without writes, uses only its key and merges latest peer state",
    "Store rejects unknown/nonfinite/accessor/prototype/malformed nested mutations without writes",
    "Malformed stored config is recovered without mutation or accessor execution",
    "Write failures return a truthful transient candidate; failed reads never overwrite storage",
    "Engine module has no own DOM/render/audio/RAF/network side effects"
  ],
  "engineSha256": "15556670c85b51a4d41c3f998e05eaff82703c345dcbea6e8a0b699e18e0d4b4",
  "schemaSha256": "cae11fa5b379256b0a586fa94f69615da081d109bac7d4ff3b414608594b09ed",
  "limit": "Actual pure-module function evidence only. Parent must verify integrated real audio, controls, storage and rendered scenes."
}
