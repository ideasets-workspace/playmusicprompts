# Immersive Lab revision 2

The owner rejected the initial lab: the same player interface and one simple effect did not satisfy the request. Initial visual acceptance FAILED. Increasing particle counts alone cannot repair that gap. Original website_html_templates remains read-only.

The deliverable is now a genuinely different full-screen player in website_html_templates_lab. Existing audio engine, queue and advanced drawers remain working, but the front-stage layout is redesigned around inhabitable environments. Large uninterrupted WebGL viewport, fine editorial typography, a three-world selector, translucent bottom transport and an integrated sound/visual control rail. No old left marketing column, no tiny object in a blank main pane. English copy, all local.

Three new environments: Tidal, an endless spectral ocean and luminous horizon; Monolith, monumental spatial architecture with moving emissive seams and a composed camera; Aether, an immersive surrounding nebular light field with layered near/far particles and volumetric illumination. All are actual Three.js environments responding to real audio bands/transients. These are artistic authored environments, not a world-first or physically exact simulation claim.

Common asset API: createXAsset(THREE), except Aether also receives {renderer}. Return group, view, update(frame), setPalette(world), diagnostics(), dispose(). Host adds group.y=2.75. Camera far=100; own geometry bounded within about70 units. view can specify aspectFit:false for a composed immersive view, orbit:false if necessary; use global target and normalized direction, distance/minDistance/maxDistance. Host owns clock, camera, renderer, audio, palettes, global grade and quality; no new RAF/context/audio. Frame supplies dt already scaled by global motionSpeed, motion flag, active flag, gated bass/mid/treble/energy, real spectrum64 and onset/pulse. Motion off freezes ALL automatic motion/shader time, with explicit controls still effective. Low quality must reduce actual work. Sanitize incoming finite values, dispose all owned resources.

Shared controls frame.labLook: form silk/nova/helix (authored scene variation), flow0..2, spread.6..1.5, turbulence0..1.5, glow.2..1.8, depth0..1, focus0..1, journey boolean. Make each visual control produce a perceptible relevant effect or explicitly scope it. Existing29global controls preserved. Lab front-stage default Tidal; four original worlds remain selectable in the copied Studio collection.

LAB-02 remains open. Required subwork: revised layout, Tidal scene, Monolith scene, Aether environment, 3-world selection/control/Director integration, initial real-render assessment for each. Close only after actual scope-matching inspection and repairs. LAB-03 then verifies integrated playback/freeze/mute/seek/quality/capture/resize, preservation of all227 original files, independent stores and finalhandoff. Owner artistic acceptance stays distinct from technical verification.
